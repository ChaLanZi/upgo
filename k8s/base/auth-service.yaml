apiVersion: v1
kind: Service
metadata:
  name: auth
  labels:
    app: auth
spec:
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
    - name: health
      port: 9090
      targetPort: 9090
  selector:
    app: auth
